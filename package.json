{
  "name": "techmart-ai",
  "version": "1.0.0",
  "private": true,
  "description": "TechMart AI — Full-stack tech e-commerce platform",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "db:setup": "psql techmart < server/migrations/001_initial_schema.sql && psql techmart < server/migrations/002_seed.sql"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cloudinary": "^2.9.0"
  }
}
